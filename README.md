# SHL AI Assessment Recommender

Single deployable FastAPI full-stack app for SHL conversational assessment recommendations.

- Main app code: `backend/`
- One-click Render blueprint: `render.yaml`
- Root Docker image entrypoint serves API + frontend from one service.

See full setup and architecture details in `backend/README.md`.
